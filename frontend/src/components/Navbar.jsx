const Navbar = () => {
  return (
    <div
      style={{
        height: "60px",
        background: "#0f4c81",
        color: "#fff",
        display: "flex",
        alignItems: "center",
        padding: "0 20px",
        fontSize: "18px",
        fontWeight: "bold",
      }}
    >
      VisionBridge Ventures
    </div>
  );
};

export default Navbar;